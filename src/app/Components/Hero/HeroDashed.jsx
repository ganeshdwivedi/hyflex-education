import React from 'react'

const HeroDashed = ({ className }) => {
    return (
        <div className={`border absolute ${className} border-[#7077E4] rounded-md p-2 border-dashed`}>
        </div>
    )
}

export default HeroDashed